# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ── Projektspezifisch ────────────────────────────────────────────────
# Interner Business Case: enthält Vertriebsstrategie, Budgets und
# Wirtschaftlichkeitsrechnung der Kundin. Das Repo ist öffentlich —
# das Dokument gehört nicht hinein.
AlpenDry_Business_Case.docx
*.docx

# Lokale Editor-/Agent-Einstellungen
.claude/settings.local.json

# Build-Artefakt
tsconfig.tsbuildinfo

# Logo-Quelldatei (Markenasset). Die abgeleitete, freigestellte Fassung
# liegt in public/brand/mark.png und wird ausgeliefert.
Logo.png

# Original-Fotos der Kundin (WhatsApp, unkomprimiert). Die optimierten
# WebP-Fassungen liegen in public/fotos/ und werden ausgeliefert — die
# Originale gehören nicht ins öffentliche Repo.
WhatsApp Image*.jpeg

# Bildquellen für /arbeiten (je ~3 MB PNG). Ausgeliefert werden die
# 4:3-WebP in public/arbeiten/ (erzeugt mit sharp) — die Originale gehören
# nicht ins öffentliche Repo.
/Wasserschaden.jpg
/Wasserschadensanierung.png
/Leckageortung.png
/Leckageortung.webp
/Technische_Trocknung.png
/Trocknung.png
/Trocknung.webp
/Schimmelsanierung.png
/Schimmel.webp
/Hochwasser.png
/Hochwasser_Präventation.png
/Hochwasser.jpg
/Dokumentation.png
/Dokumentation.jpg
